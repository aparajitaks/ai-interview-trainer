# Frontend (React + Vite + Tailwind)

Quick start (run from project root):

1. cd frontend
2. npm install
3. npm run dev

App will be available at http://localhost:5173

This directory contains a lightweight UI-only prototype for the AI Interview Trainer.
Pages: Home, Interview, Result. No backend or camera access enabled yet.
